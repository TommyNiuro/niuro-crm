{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permisos base de la ventana principal. El CRM se sirve por HTTP local, no usa IPC de Tauri.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
